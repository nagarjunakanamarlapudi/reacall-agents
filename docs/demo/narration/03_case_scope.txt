The user opens the pinned H-1230-2026 recall. The header exposes the official openFDA snapshot, the durable case and thread identifiers, and the OpenAI GPT-5 mini reasoning mode. The investigation scope is deliberately small and visible: four selected lots out of 144 available records. They represent an exact match, a probable match, an ambiguous record, and a rejected record. Scope becomes immutable when the run starts. The public recall and the synthetic Northstar operations evidence remain visibly separated, so the application never implies that the fictional organization participated in the real recall.
