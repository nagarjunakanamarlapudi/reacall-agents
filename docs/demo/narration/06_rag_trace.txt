The lower trace makes the evidence path auditable. Agentic RAG issued a bounded question for affected lots and facilities, ran sparse and dense retrieval, fused and reranked the candidates, and recorded each hop. The critic detected stalled progress after the permitted rewrite and stopped safely. No unsupported evidence was promoted into an operational fact. The same screen connects the live plan, token and latency telemetry, model and MCP tool calls, the fail-closed outcome, and the retrieval trace. That end-to-end trace is the difference between a chatbot answer and an inspectable agent workflow.
